import React from "react";
import ReactDOM from "react-dom/client";
import NecAcademieApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NecAcademieApp />
  </React.StrictMode>
);
